# Default values for poclistener.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: dhanapodigiri/poclistener 
  tag: "1.0.0-42"
  pullPolicy: Always

imagePullSecrets: []
nameOverride: "poclistener"
fullnameOverride: "poclistener"

